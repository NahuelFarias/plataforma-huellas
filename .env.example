# Copiar a .env y ajustar si hace falta.
# Prisma requiere MongoDB como replica set (ver docker-compose.yml). Tras `docker compose up -d`, esperar a que mongo-init termine.
DATABASE_URL="mongodb://127.0.0.1:27017/plataforma_huellas?replicaSet=rs0&directConnection=true"
